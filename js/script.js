
function openNav() {  // define the element by #id(0px of width by default); give it with while open;
    document.getElementById("burger").style.display = "flex";
  }
  
function closeNav() {  // define the same element by #id. Give it 0px of width (hide) after clicking on button
    document.getElementById("burger").style.display = "none"; 
}
